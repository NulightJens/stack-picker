# Stack Picker

Interactive picker for building your 2026 app stack or content operation. Pick tools layer by layer, then copy the prompt, download a PNG, or export a Mermaid diagram.

```bash
npm install
npm run dev
```

Visit http://localhost:5173 — frontend-only. For the email-capture API, see `CLAUDE.md` for Wrangler/D1 setup.
